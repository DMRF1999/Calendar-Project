<!DOCTYPE html>
<html>
  <head>
    <title>Index Page</title>
  </head>
  <body>
    <h1>All my Events!</h1>
    <a href="/events/new">Create a new Event</a>

    <ul>
    <% allEvents.forEach((ev) => { %>
      <a href="/events/<%= ev._id %>">
        <li>Event: <%= ev.title %> Date: <%= ev.date %> Description: <%= ev.description %> </li>
      </a>
    <% }) %>
  </ul>


  </body>   
</html>